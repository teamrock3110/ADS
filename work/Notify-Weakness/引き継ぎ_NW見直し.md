# 次のチャットへの引き継ぎ：NW 脆弱性管理の見直し（2026-09-13）

**この順で読むこと。** ① この文書 → ② `README.md`（§0・§1.5・**§4 はコードを触る前に必読**）
→ ③ `社内ルール案_OS更新基準.md`（判定基準の正典）→ ④ GAS の `fortinet_psirt_watcher_v7_readme.gs`（運用手順）

**見直しの範囲はまだ決まっていない。**利用者は「NW 脆弱性管理の見直しをする」とだけ言っている。
**最初に何を見直すのかを聞くこと。**この文書は材料であって計画ではない。

---

## 0. 最初に知っておくこと（ここを飛ばすと事故る）

### 0-1. 本番 GAS に入っているコードは main ではない

| | 本番 GAS | `main` |
|---|---|---|
| `fortinet_psirt_watcher_v7.gs` | **`feat/macos-release-monitor` 版**（4,721 行・181 関数） | 4,613 行・178 関数 |
| `macos_release_monitor.gs` | 入っている | **無い** |
| `readme.gs` | §6 に macOS の運用手順あり | 無い |

macOS 対応で v7.gs に **接続点 4 か所**を入れ、それが本番で動いている（§2）。
ブランチは push 済みだが **PR 未作成・未マージ**（2026-09-11 に PR を作るか聞いたところ「いいえ」。恒久的な判断かは未確認）。

**main から見直しのブランチを切ると、本番と違うコードを読んで判断することになる。**
作業を始める前に、どのブランチを起点にするか（先にマージするか）を利用者に確認すること。

### 0-2. README の数字と参照が古い

**README 自身が「§5 を見よ」と言っている場所が古い。**見直しで最初に踏む。

| 場所 | 書いてあること | 実物 |
|---|---|---|
| §0 / §5 | 本体 5,333 行・202 関数、tests 708 行・25 関数 | 本番 4,721 行・181 関数、tests 664 行・24 関数 |
| §1.3 表・§1.4-8・§4.16 | 「§6-2」「§6-3」を参照 | §6 の小節は「1. プレ運用の観測」「2. 10 月の新運用」の 2 つだけ。**参照先が存在しない** |
| §1.3 CISA KEV | CacheService に 6 時間キャッシュ | キーは `kev_catalog_v2`、GitHub ミラーへの退避あり（§2）。**README に記載なし** |
| 全体 | macOS への言及ゼロ | Gemini 枠を macOS と共有している（§2） |
| `readme.gs` | 「6. コードを直す人へ」と「6. macOS リリース監視」 | **節番号 6 が 2 つある** |

どれも本チャットでは直していない。

### 0-3. スプレッドシートは Claude から見えない

台帳・処理済み・実行履歴・資産の中身は、**利用者にスクリーンショットか関数の実行ログを貼ってもらう。**
推測で「動いているはず」と言わないこと（本チャットで、画像からは列のずれを判別できず、
診断関数の出力で初めて分かった件がある）。

---

## 1. NW ツールの現状（30 秒）

毎朝 9 時台に Fortinet / Cisco の CSAF を取り、自社資産に当たる CVE × 製品だけ台帳に書き、
Slack へ 1 通。**判定はコード、LLM は日本語化だけ。**詳細は README §1。

- **プレ運用中**（2026-09-06 開始、1 週間経過）。README §6 は **「しばらくコードを足さないこと。不満が出てから直す」**
  → **見直しがコード変更を伴うなら、この方針と衝突する。**そこも含めて利用者に確認
- 観測中の問い: 「対応検討」が毎月何件出るか。8 月は 7 件（Cisco の Security Hardening Release が重なった月の可能性）。**1〜2 か月見ないと分からない**
- シート 5 枚（台帳 14 列 / 処理済み 10 列 / 実行履歴 11 列 / 資産 9 列 / 判断記録 7 列）
- デプロイは GAS エディタへの**手貼り**（clasp は見送り済み・再提案禁止）

---

## 2. macOS 対応で NW 側に入ったもの（本番で動いている）

判定ロジック 9 関数には触っていない。接続点は 4 か所（コミット `a603ad3`）。

| 接続点 | 内容 | NW 見直しで注意すること |
|---|---|---|
| KEV 取得 | `kevCatalogWithStatus_()` を新設し、`fetchKevCatalog_()` はその薄いラッパ（外形維持）。キャッシュキー `kev_catalog_v2`。CISA 不通時は GitHub ミラーへ退避 | **戻り値の形を変えるならキャッシュキーも変える。**変えないと最大 6 時間、旧形のキャッシュが返り `isKevListed_` が全件 false になる（例外も握りつぶすのでログに残らない） |
| Gemini 呼び出し | `callGemini_(prompt, responseSchema)` / `callGeminiModel_(model, prompt, responseSchema)`。省略時は従来どおり | 引数を増減するなら macOS 側の呼び出しも壊れる |
| AI 消費の計数 | `countAiRequest_()` がスクリプトプロパティ `AI_COUNT_yyyyMMdd` にも積む。macOS と共有 | **NW が 9 時台、macOS が 10 時台なので NW が先に枠を取る。**macOS は当日合計 12 回（`MACOS_AI_DAILY_BUDGET`）で AI を止める。**NW の AI 使用量を増やす変更は、macOS の要約が付かない日を増やす** |
| 実行履歴の備考 | 「KEV照合不可 / KEV出典」と「macOS監視：最終実行から N 日」 | macOS のトリガー消滅に気づく唯一の経路。`writeRunLog_` を触るなら残すこと |

---

## 3. 未解決（README §6 と前回の引き継ぎから。**2026-09-07 時点の記載**）

**本チャットではどれも進めていない。進捗は利用者に確認すること。**

| 課題 | 状態 |
|---|---|
| 社内ルール案の室長承認 | 条件 4（CVSS ベクター）は 09-04 に固まり、KEV（§5）は 09-01 承認済み。「出せる状態」。**10 月の新運用の前提** |
| 資産棚卸しの運用ルール | `更新日` 列はあるが、いつ誰が確認するか未定。登録漏れは静かな見逃しになる（README §4.16） |
| 判断記録を誰がいつ書くか | シートはあるが運用が無い |
| **Gemini 無料枠のデータ利用** | 無料枠は入力が Google の製品改善に使われる。「実データを入れる前に有料化を判断する」としていたが**判断の記録が無いまま運用中**。macOS も相乗りしたので対象が増えている |
| FortiClient の前提確認 | 「EMS 経由の自動更新が別にある」前提で対象外にしている |
| Cisco `vulnerabilities[].title` | 複数 CVE のアドバイザリで CVE ごとに題名が違うが、`document.title` を全行にコピーしている（README §1.3。未対応） |

---

## 4. macOS 対応で利用者から受けた FB（NW 見直しにそのまま効く）

**いちばん強かったのは「不要な要件が積み重なっている。シンプルに通知してほしいだけ」。**

| FB | 何が起きたか | NW で疑うこと |
|---|---|---|
| **要件でないものを積まない** | 「配り終わったら追跡状態に適用済と書く」運用を入れていた。出どころは引き継いだ仕様書で、**利用者は一言も言っていなかった**。実測すると通知量にも効いていなかった | NW の機能・列・運用ごとに「利用者が求めたものか、引き継ぎや先回りで入ったものか」。前回も 5,912 → 4,614 行の削減が「使ってる？」と聞いて出た |
| **毎回出る注意書きは読まれない** | 🟢 の通知に毎回「安全の保証ではない」と出していた。守りたい相手（ヘッダだけ読む人）には届かず、読む人には数回で背景になる。削除し、読み方は readme.gs に寄せた | NW の Slack 文面。**v7.gs に `※`・「保証」・「安全の」を含む文面は無い**（grep で確認）。ただし NW の通知文面そのものは本チャットでは読んでいない |
| **2 秒で分かる通知** | 注意書きをリード文に統合する案を出したら「パッと理解できない」と却下された（33 字 → 70 字） | 文を足して解決しようとしない |
| **何を聞いているのか 1 行で** | 選択肢を並べて「決めてください」と返したら「何の判断を聞かれている？」「話長すぎ」 | 判断を求めるときは問いを 1 行で。**確かめれば分かることを意見として投げない** |
| **確かめてから言う** | 「実悪用の正規表現は静かに壊れうる」と成功例を 1 件も見ずに言った。直近 40 リリースを全取得したら 6/6 拾えていた。撤回した | 心配を口にする前にデータを取る |
| **自分の出した指示も疑われる** | 台帳の 4 セルを手で書き換えるよう指示したら「意味あるの？」。判定に効かず 90 日で消える値だった。撤回した | 人に作業を頼む前に、やらなかったら何が起きるかを数字で確かめる |
| **シートを手で手術させない** | 台帳の列を手で削除するよう指示し、ヘッダとデータがずれた（実行は成功して見える型）。シートごと作り直して解消 | 列を変えるときは作り直す前提で手順を出す |

---

## 5. 利用者の作法（継続）

- **次の作業は必ず聞いてから動く。**勝手に機能追加しない
- **結論ファースト。短く。**手順の羅列・過度な言い換えは不要
- **「なぜその値・その設計か」を必ず問われる。**根拠が無ければ「根拠はない」と言う
- **「パッチワークになっていないか」「本質からずれていないか」を問われる**
- **知らないことは調べる。推測で答えない**
- **行数や工数を、依存関係を確認せずに言わない**（前回「449 行削れる」→ 実績 147 行）
- **忖度のない FB を求められる。**ただし利用者の指摘のほうが正しかったことが何度もある。データで確かめてから反論する
- コミット・push は依頼があるまでしない
- チームは git を見られない。**運用に必要なことは GAS の `readme.gs` に書く。**README.md に運用手順を書かない

---

## 6. 触ってはいけないもの・再提案しないもの

README §6「追わないと決めたもの」に理由がある。とくに:

- **clasp**（2026-09-01 見送り）/ **JPCERT を判定に混ぜる・別シートを作る** / **月次サマリ・集計機能**（09-07 削除。「求めていない機能を入れられた」と明言）
- RSS の `Revised on` を判定に使う / アドバイザリ HTML の取得 / 条件付き GET / Cisco を全件取得にする
- openVuln 再導入 / Slack Bot・ボタン・画像

**判定ロジック 9 関数**（`decideNotification_` / `ruleGate_` / `impactSeverity_` / `matchesSpec_` /
`normProduct_` / `isUnauthRemote_` / `narrowFixVersion_` / `finalizeVerdict_` / `featureExposure_`）は
出力が変わる。**変える前に、変更前の挙動を全パターン記録し、実データで差分を見せて承認を得る。**
判定基準を変えるときは **社内ルール → コードの順**（README §0）。

---

## 7. 検証のやり方

- scratchpad に node のハーネスを置き、GAS の API をスタブして本体を `eval` で読み込む（セッションごとに作り直す）
- 実データは Fortinet RSS / CSAF、Cisco CSAF、CISA KEV を curl で取って使う。**作り話のデータで判定を確かめない**
- **node で 2 ファイルを連結しても GAS のファイル分離は再現できない。**別ファイルのトップレベル `const` は GAS では見えないことがある。確認用から参照する定数は `var`、`testSharedConstants()` を最初に実行
- **回帰テストは、旧コードに戻して落ちることまで確認する**

---

## 8. 状態（2026-09-13）

- リポジトリ `/Users/saitohmasakazu/src/work/Notify-Weakness`（git root は `/Users/saitohmasakazu/src`、`teamrock3110/ADS`）
- 現在のブランチ `feat/macos-release-monitor`（`ccb32bc`、origin と一致）。`main` は `c83acd5`
- 他のブランチ: `feat/slack-targets-and-counts` / `fix/fortinet-csaf-detection` は main にマージ済み。`backup-before-cleanup-20260830` は未マージ（main に無いコミット 4 件、最終 08-30。題名は「不使用コードを削除」「既読判定で CSAF 版 "0" が空文字に化ける不具合」「v7 の現状をチェックポイント」など。差分は見ていない）
- **macOS 対応は利用者が 2026-09-13 に完了と判断。**ただし本チャットでは、最終形（台帳 19 列）の貼り替え結果と、10 時台のトリガー実行の行は**確認していない**
- macOS の経緯は `引き継ぎ_macOS対応_次チャット.md` と `引き継ぎ_macOS対応_設計確定.md`
- この文書は**未コミット**

---

## 9. 見直しの進捗（2026-09-13、ブランチ `refactor/nw-align-macos`、未コミット）

利用者の指示: **「macOS の作りが気に入っているので、それをベースに NW の汚い部分を直す」。**
洗い出した差 6 つのうち、出力を変えない 1〜4 を実施した。5（判定理由を固定語の列に分ける）と
6（Slack を 1 件 1 通・1 行目に結論）は**出力が変わるので未着手。**やるなら実データで差分を見せて承認を取る。

| # | やったこと |
|---|---|
| 1 | 関数 `nw〜`・定数 `NW_〜` に改名（241 個）。`main`→`nwDaily`、`setup`→`nwSetup`、`testX`→`nwTestX`。macOS と共有する 5 つ（`postSlack_` / `kevCatalogWithStatus_` / `callGemini_` / `countAiRequest_` / `SLACK_WEBHOOK_PROP`）とその内部だけ接頭辞なし。規則は v7.gs 冒頭に明記 |
| 2 | シート名を `NW台帳` / `NW資産` / `NW処理済み` / `NW実行履歴` / `NW判断記録` に。`nwSetup()` が旧名を改名する（`nwRenameLegacySheets_`。列も行も触らない。両方あるときは改名せずログ） |
| 3 | 5 シートの列を key / label の配列（`NW_LEDGER_COLS` など）に。`COL['見出し']` と `HEADERS.indexOf('見出し')` 計 20 か所を `nwCol_(cols, key)` に置換。行の書き込みは `nwRecToRow_`、読み込みは `nwRowToRec_` |
| 4 | トップレベルの `const` 39 個・`let` 4 個をすべて `var` に（macOS と同じ。別ファイル参照の罠を全部で塞ぐ） |

**検証**: scratchpad の node ハーネス（GAS API をスタブ、3 ファイルを 1 コンテキストに読み込み）で、
改名前後のオフライン確認関数 16 本の出力が一致、実データ（Fortinet 50 / Cisco 50 の CSAF、KEV、JPCERT を
curl で取得）で `main()` → `nwDaily()` を回して **台帳 17 行・処理済み 100 行・Slack payload が完全一致**
（差は所要秒のみ）。旧名シートの改名も動作確認済み。

**貼るときの手順は readme.gs §7 に書いた。**貼ったあと `nwSetup()` を 1 回、`main` のトリガーを削除して
`nwDaily` で作り直す。この 2 つを忘れると翌朝から動かない。

**気づいたが触っていないもの**: `nwReadAssets_` に v6 の資産シート互換分岐（`isV7` 判定）が残っている。
v6 シートはもう無いはずで、消せるなら消す（利用者に「使ってる？」を聞く）。

README.md の行数・関数数（§0・§5）は今回の実物に合わせた。§1.3 の「§6-2 / §6-3」参照切れは未修正。

### 9-1. 判断記録シートを廃止（2026-09-13、コミット f22c46e の後）

貼り替え後に利用者が「判断記録」シートを見て **「そのシートはいらない」**。人の判断で判定を上書きする仕組み
（`NW_SHEET_DECISION` / `NW_DECISION_*` / `nwEnsureDecisionSheet_` / `nwReadDecisions_` / `nwLookupDecision_` /
`nwApplyHumanDecision_`）をコードごと外し、`nwDecideNotification_` はルール判定だけにした。シートは 4 枚。
readme.gs は §4 を削って 8 節に、README §2.5 / §4.13 は廃止の記録に置き換えた。
ハーネスで再検証: 出力は判断記録シートが無くなった以外すべて一致。
**本番の「NW判断記録」シートは空なので、手で消してよい（ツールはもう読まない）。再提案しないこと。**

### 9-2. 本番反映（2026-09-13）

利用者が 3 ファイルを GAS へ貼り、`nwSetup()`・トリガー張り替え（`main`→`nwDaily`）・確認関数・`nwDaily()` を
実行して **「問題なし」と報告。**本番 GAS は `bb0ecaf` 相当。旧「判断記録」シートは削除してよいと伝えた。
残る候補は §9 冒頭の 5・6（出力が変わる）と、`nwReadAssets_` の v6 互換分岐の削除。いずれも利用者の指示待ち。

### 9-3. v6 互換分岐の削除（2026-09-13）

`nwReadAssets_` の v6 資産シート互換読みを削除。見出し 1 列目が「ベンダー」でなければ例外で止める
（黙って別解釈で読まない）。ハーネスで出力一致を確認。残り候補 5・6 は「今はやらない」で利用者と合意。

### 9-4. Gemini の呼び出し経路を切り替え可能に（2026-09-13）

別セッションで会社側 GAS（GCP `it-dx-prod`）に Vertex AI 経由の呼び出しを入れたが、その変更は git に無く、
会社側の `nw.gs`（**改名前の v7 に Vertex 対応だけ足したもの**）にしか無い。会社側は `test.gs` だけ改名後で
本体が改名前という食い違いが起きている（引き継ぎ「既知の残課題」）。

こちらの本体に同じ設計を入れた: `GEMINI_BACKEND`（`'aistudio'` 既定 / `'vertex'`）と `GCP_PROJECT_ID`、
`buildAiStudioRequest_` / `buildVertexRequest_`（`callGeminiModel_` はリトライと解析だけ）、
`shouldFallbackGeminiModel_` / `geminiRetryWaitMs_` の RESOURCE_EXHAUSTED 対応、ログ「AI 生成: gemini（vertex）」。
`appsscript.json`（cloud-platform スコープ入り）を git に置き、readme.gs §3・§6 に手順を書いた。
ハーネスで両経路の URL・認証ヘッダ・safetySettings・フォールバック判定を確認。aistudio の実データ出力は不変。

**会社側 GAS の食い違いを解消する手順**: main の 3 ファイル＋macOS を貼り、`GEMINI_BACKEND` を `'vertex'` に変え、
`nwSetup()`・トリガー張り替え（`main`→`nwDaily`）・`nwTestSharedConstants()`（15 / 15）・`nwTestAi()`。
会社側で足した簡易 `testAi()` は不要になるので消す。会社側の共有ユーザで macOS 側（`macosSelfTest()`）の確認は未実施。
